import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  imports: [],
  templateUrl: './app.html',
  styleUrl: './app.css',
})
export class App {
  protected readonly title = 'Sample Copilot';
  protected readonly description =
    'A basic Angular app that is ready to build and deploy.';
}
